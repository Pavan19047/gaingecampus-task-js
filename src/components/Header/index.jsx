import React from 'react';
import { SHeader, SHeaderTitle } from './styled';

const Header = () => {
  return (
    <SHeader>
      <SHeaderTitle>AI Chat Page</SHeaderTitle>
    </SHeader>
  );
};

export default Header;
